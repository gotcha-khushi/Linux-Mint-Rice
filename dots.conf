conky.config = {
    -- `center` is not a Conky alignment value; this centers the window both ways.
    alignment = 'middle_middle',
    background = false,
    border_width = 0,
    cpu_avg_samples = 2,
    default_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = false,
    draw_outline = false,
    draw_shades = false,
    gap_x = 0,
    gap_y = 0,
    -- 1920x1080 display, with a 20 px margin on every edge.
    minimum_height = 1040,
    minimum_width = 1880,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    extra_newline = false,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'desktop',
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    update_interval = 0.03,

    lua_load = '~/.config/conky/dots.lua',
    lua_draw_hook_pre = 'draw_dots',
}

conky.text = [[
]]
